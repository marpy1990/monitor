<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sjtu.cit.monitor.dal.dao.IconDao">
	<select id="getSourceIcon" parameterType="map" resultType="String"><![CDATA[
		select
			URL
		from
			SOURCE_ICON
		where
			SOURCEID=#{sourceId}
		and FEATURE|#{feature}=#{feature}
		order by
			FEATURE desc
		limit 1
	]]></select>
	
</mapper>
